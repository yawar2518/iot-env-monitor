# =============================================================
# Base Requirements — installed in all environments
# =============================================================

# -------------------------------------------------------------
# Django Core
# -------------------------------------------------------------
Django==5.0.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-filter==24.2

# -------------------------------------------------------------
# ASGI Server + Django Channels (WebSocket support)
# -------------------------------------------------------------
daphne==4.1.2
channels==4.1.0
channels-redis==4.2.0

# -------------------------------------------------------------
# Database — PostgreSQL + TimescaleDB
# -------------------------------------------------------------
psycopg2-binary==2.9.9
django-timescaledb==0.2.1

# -------------------------------------------------------------
# Celery — Task queue and scheduler
# -------------------------------------------------------------
celery==5.4.0
django-celery-beat==2.6.0

# -------------------------------------------------------------
# MQTT Client — connects to Mosquitto broker
# -------------------------------------------------------------
paho-mqtt==1.6.1

# -------------------------------------------------------------
# Utilities
# -------------------------------------------------------------
# Reads .env files and environment variables cleanly
python-decouple==3.8

# Serves static files without a separate nginx in development
whitenoise==6.7.0